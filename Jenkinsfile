pipeline {
  agent any
  stages {
    stage ("Welcome to Jenkins") {
      steps {
        script {
          var1=input 'Enter a value'
		  println "Value of var1 is ${var1}"
        }
      }
    }
  }
}
